import { jsonEndpoint } from '../../../helpers/json-endpoint';
import { module, test } from 'qunit';

module('Unit | Helper | json endpoint');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = jsonEndpoint(42);
  assert.ok(result);
});
