import { geoJSONIo } from '../../../helpers/geo-json-io';
import { module, test } from 'qunit';

module('Unit | Helper | geo json io');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = geoJSONIo(42);
  assert.ok(result);
});
