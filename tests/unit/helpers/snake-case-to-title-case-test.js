import { snakeCaseToTitleCase } from '../../../helpers/snake-case-to-title-case';
import { module, test } from 'qunit';

module('Unit | Helper | snake case to title case');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = snakeCaseToTitleCase(42);
  assert.ok(result);
});
