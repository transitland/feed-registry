import { geojsonEndpoint } from '../../../helpers/geojson-endpoint';
import { module, test } from 'qunit';

module('Unit | Helper | geojson endpoint');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = geojsonEndpoint(42);
  assert.ok(result);
});
