import { mobilityExplorerLink } from '../../../helpers/mobility-explorer-link';
import { module, test } from 'qunit';

module('Unit | Helper | mobility explorer link');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = mobilityExplorerLink(42);
  assert.ok(result);
});
