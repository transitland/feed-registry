import { sspEndpoint } from '../../../helpers/ssp-endpoint';
import { module, test } from 'qunit';

module('Unit | Helper | ssp endpoint');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sspEndpoint(42);
  assert.ok(result);
});
