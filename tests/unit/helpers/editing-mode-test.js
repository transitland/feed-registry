import { editingMode } from '../../../helpers/editing-mode';
import { module, test } from 'qunit';

module('Unit | Helper | editing mode');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = editingMode(42);
  assert.ok(result);
});
