import { removeUnderscore } from '../../../helpers/remove-underscore';
import { module, test } from 'qunit';

module('Unit | Helper | remove underscore');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = removeUnderscore(42);
  assert.ok(result);
});
