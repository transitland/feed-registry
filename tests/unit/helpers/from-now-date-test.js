import { fromNowDate } from '../../../helpers/from-now-date';
import { module, test } from 'qunit';

module('Unit | Helper | from now date');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = fromNowDate(42);
  assert.ok(result);
});
