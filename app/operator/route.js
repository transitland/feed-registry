import Ember from 'ember';

export default Ember.Route.extend({
  // model: function(params) {
  //   return Ember.RSVP.hash({
  //     operator: this.store.query('operator', { name: params.operator.name }).then((operator) => {
  //       return operator.get('firstObject');
  //     })
  //   });
  // }
});
