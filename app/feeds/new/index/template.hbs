{{#if isLoading}}
	{{loading-spinner progress=progress progressStatus=progressStatus}}
{{/if}}

<div class="fullvw-wrapper">
	{{add-feed-tabs header-text="ADD A TRANSIT FEED" upload-tab-class="tab current" add-operator-tab-class="tab" license-tab-class="tab" submit-tab-class="tab" submitted=false}}
</div>
<div class="container">
  <div class="add-operator-info">
    <p>To add a feed to <a href="https://transit.land/news/2019/10/17/tlv2.html">Transitland v2</a>, the current version of Transitland, please see the <a href="https://github.com/transitland/transitland-atlas#readme">Transitland Atlas repository on Github</a>.</p>
  </div>
  <!-- Tlv1:
	<div class="add-operator-info">
		<p>
		Transitland brings together many sources of transit data to build a <a href="https://transit.land/feed-registry" target="_blank">directory of operators and feeds</a> that can be edited by transit enthusiasts and developers. Your contributions can help expand Transitland's coverage throughout the world.
		</p>
		<p>
		If you know of a feed, please continue below!
		</p>
		<p>
			{{documentation-link "feed-registry/add-a-feed.html#feedurl" "Learn more about GTFS feeds"}}
		</p>
	</div>
	<form {{action "next" on="submit"}}>
		<div class="input-form-wrapper">
			{{#if model.errors}}
					{{#each model.errors as |error|}}
						<div class="error-tip">
							<i class="fa fa-exclamation-circle"></i> {{error.message}}
						</div>
					{{/each}}
			{{/if}}

			<div class="row {{if model.errors.url "error"}}">
				<div class="col-sm-12">
					<div class="input-form-label">
						{{#if isLoading}}
							{{progressStatus}}
						{{else}}
							Paste the GTFS feed URL
						{{/if}}
					</div>
					<div class="input-form-field">{{input type="text" value=model.url placeholder="http://... .zip" autofocus=true}}</div>
				</div>
			</div>
		</div>

		<div class="caption">
      After this feed is added to Transitland, our servers will periodically check this URL for updates.<br/>
      If you represent a transit agency that is unable to host your own feed file, please follow our <a href="https://github.com/transitland/gtfs-archives-not-hosted-elsewhere/blob/master/README.md#how-to-submit-a-new-or-updated-feed-archive">instructions to submit your feed via e-mail</a>.<br/>
      To edit the record for a feed already in the Feed Registry, paste its URL above and press Next.
		</div>

		{{#if model.url}}
			<div class="row direction-btn-wrapper">
				<div class="col-sm-12">
					<div class="pull-right">
					 <a class="btn btn-action" role="button"  type="submit" {{action "next"}}> Next <i class="fa fa-chevron-right"></i></a>
					</div>
				</div>
			</div>
		{{else}}
			<div class="row direction-btn-wrapper">
				<div class="col-sm-12">
					<div class="pull-right">
					 <a class="btn btn-action" disabled role="button"  type="submit" {{action "next"}}> Next <i class="fa fa-chevron-right"></i></a>
					</div>
				</div>
			</div>
		{{/if}}
	</form>
  -->
</div>
