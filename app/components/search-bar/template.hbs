{{#power-select
  options=places
  onopen=(action "findPlaces")
  searchPlaceholder="Type to filter..."
  selected=place
  onchange=(action (mut place))
  as |location|
}}
  {{location}}
{{/power-select}}