  {{! NOTE: if an operator happens to have a name that is an exact match with an
  ISO 3166-1 or 3166-2 code, then it will be converted. For example, if an operator
  has the name "US" it will be listed as "United States"--hopefully this will
  never happen! }}

{{#power-select
  selected=placeOrName
  options=placesAndNames
  autofocus="autofocus"
  searchField="display"
  onopen=(action "findPlacesAndNames")
  placeholder="Search by operator name or location"
  searchPlaceholder="Type to filter..."
  onchange=(action "setPlaceOrName")
  as |placeOrName|
}}
  {{placeOrName.display}} <span class="place-type-label">({{placeOrName.type}})</span>
{{/power-select}}

{{#if placeOrName}}
  <div class="filterInfoContainer">
    <div class="filterInfoSubcontainer">
      <div class="filterInfo">There {{#if multipleOperators}}are{{else}}is{{/if}} currently <strong>{{totalOperators}}</strong> operator{{#if multipleOperators}}s{{/if}} available in the Transitland Datastore with {{placeOrName.typeToDisplay}} of <strong>{{placeOrName.display}}</strong>.</div>
    </div>
  </div>
{{/if}}
